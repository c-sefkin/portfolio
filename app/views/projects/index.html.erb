<% content_for(:title, "Home | To Do") %>

<h1>Projects</h1>

<ul>
  <% @projects.each do |project| %>
    <li><%= link_to project.name, project_path(project) %></li>
  <% end %>
</ul>

<%= link_to "Add project", new_project_path, :class=> "btn btn-default" %>
